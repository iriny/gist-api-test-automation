# GitHub personal access token (classic) with the "gist" scope.
# Create one at https://github.com/settings/tokens -> Tokens (classic) -> Generate new token (classic).
GITHUB_TOKEN=

# GitHub REST API base URL. Override only for enterprise/self-hosted instances.
BASE_URL=https://api.github.com
